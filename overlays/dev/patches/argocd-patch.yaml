apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd
  namespace: argocd-dev
spec:
  source:
    helm:
      values: |
        server:
          extraArgs:
            - --insecure
          ingress:
            enabled: true
            annotations:
              kubernetes.io/ingress.class: nginx
              cert-manager.io/cluster-issuer: letsencrypt-staging
            hosts:
              - host: argocd-dev.example.com
                paths:
                  - path: /
                    pathType: Prefix
            tls:
              - secretName: argocd-dev-tls
                hosts:
                  - argocd-dev.example.com
        configs:
          secret:
            argocdServerAdminPassword: "$2a$10$DevHashedPasswordHere"
        dex:
          enabled: false
        notifications:
          enabled: true
        applicationSet:
          enabled: true
  destination:
    namespace: argocd-dev 