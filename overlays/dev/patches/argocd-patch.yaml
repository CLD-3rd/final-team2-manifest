apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd
  namespace: argocd-dev
spec:
  source:
    helm:
      values: |
        server:
          extraArgs:
            - --insecure
          ingress:
            enabled: true
            annotations:
              kubernetes.io/ingress.class: nginx
              cert-manager.io/cluster-issuer: letsencrypt-staging
            hosts:
              - host: argocd-dev.goteego.store
                paths:
                  - path: /
                    pathType: Prefix
            tls:
              - secretName: argocd-dev-tls
                hosts:
                  - argocd-dev.goteego.store
        # Secret 참조 설정
        secret:
          existingSecret: argocd-dev-secret
        dex:
          enabled: false
        notifications:
          enabled: true
        applicationSet:
          enabled: true
  destination:
    namespace: argocd-dev 