apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd
  namespace: argocd-prod
spec:
  source:
    helm:
      values: |
        server:
          extraArgs:
            - --insecure
          ingress:
            enabled: true
            annotations:
              kubernetes.io/ingress.class: nginx
              cert-manager.io/cluster-issuer: letsencrypt-prod
            hosts:
              - host: argocd.example.com
                paths:
                  - path: /
                    pathType: Prefix
            tls:
              - secretName: argocd-prod-tls
                hosts:
                  - argocd.example.com
        configs:
          secret:
            argocdServerAdminPassword: "$2a$10$ProdHashedPasswordHere"
        dex:
          enabled: false
        notifications:
          enabled: true
        applicationSet:
          enabled: true
  destination:
    namespace: argocd-prod 